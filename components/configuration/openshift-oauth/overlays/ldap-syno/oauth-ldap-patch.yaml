- op: add
  path: /spec/identityProviders/-
  value:
    mappingMethod: claim
    name: LDAP Authentcation
    type: LDAP
    ldap:
      attributes:
        email:
        - mail
        id:
        - dn
        name:
        - cn
        preferredUsername:
        - uid
      bindDN: uid=bind,cn=users,dc=lan,dc=stderr,dc=at
      bindPassword:
        name: ldap-secret
      url: ldaps://data.lan.stderr.at/cn=users,dc=lan,dc=stderr,dc=at?uid
      filter: "memberOf=cn=openshift,cn=groups,dc=lan,dc=stderr,dc=at"
      insecure: false
      ca:
        name: ldap-ca-cert
